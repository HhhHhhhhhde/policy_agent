Prompt:
请使用 Vue 3 (Setup) 和 Element Plus 生成项目的主布局组件。

设计规范：

主色调：深蓝色 #1d3557（代表金融权威性），辅助色 #457b9d，警示色 #e63946。

背景：采用微灰渐变 #f8f9fa，卡片采用纯白阴影。

顶部状态栏（Header）：

左侧：Logo + 标题“央行经济智能体 (CB-EA): 金融稳定沟通优化系统”。

右侧：状态指标卡（显示：模拟交易员数量：30位；基准利率：5.25%；当前波动率指数：中低）。

左侧导航（Sidebar）：

图标 + 文字：智能对话 (Chat)、分歧度仪表盘 (Analytics)、仿真参数设置 (Simulation)、研究背景 (About)。

数据注入（Mock）：

在 Pinia 中初始化一个状态，存储当前处理的文本状态（未处理/已优化）。

HCI 原则：确保侧边栏可折叠，主区域有面包屑导航。